# Copia in .env e compila i valori
# In produzione su Render usa le env vars della dashboard

# Chiave segreta per firmare i JWT — cambiala con un valore casuale lungo
JWT_SECRET=cambia-questo-valore-con-qualcosa-di-lungo-e-casuale

# Porta locale (Render usa la sua PORT automaticamente)
PORT=5000
